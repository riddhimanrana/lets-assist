{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/api/cron/generate-recurring-projects",
      "schedule": "0 1 * * *",
      "comment": "Primary: Daily project generation (can tolerate hourly precision)"
    },
    {
      "path": "/api/cron/auto-publish-hours",
      "schedule": "0 3 * * *",
      "comment": "Backup: Fallback for GitHub Actions auto-publish"
    }
  ]
}

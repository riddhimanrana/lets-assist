# Residual risk register

| Severity | Risk or gate | Current containment | Completion condition |
|---|---|---|---|
| P1 gate | Live local Google OAuth and Picker behavior is not accepted because `http://localhost:3001` and its callback have not been action-time verified on the external OAuth client. | No Cloud Console mutation, consent, Picker selection, Drive read/import, refresh, reconnect/revocation, or Google write was performed. The locally tested credential model binds organization, plugin, purpose, and capability and requires legacy reconnect. | Explicitly authorize the JavaScript origin `http://localhost:3001` and redirect `http://localhost:3001/api/calendar/google/callback`, then complete consent, Picker, refresh, reconnect, revocation, 403, and 429 verification. |
| P1 gate | The connected Chrome account is not proof that imports are authorized as the intended CSF Google identity. | Real import commit remains blocked; no roster rows were read or captured. | Visibly confirm `dvhighcsf@gmail.com` in the product before any private import. |
| P1 gate | Persistent Supabase `development` branch has an ongoing `$0.01344/hour` cost and has not been provisioned. | All current data and testing remain in a uniquely named isolated local stack; production is not targeted. | Obtain explicit cost confirmation, create the data-less development branch, replay migrations, and configure Preview-only variables. |
| P1 gate | CI cannot initialize and validate the private plugin without `PRIVATE_SUBMODULE_TOKEN`. | The exact private gitlink and 268-test private-plugin suite are verified locally; no CI success is claimed. | Configure the repository secret, rerun the exact-gitlink and test jobs, and verify all required checks. |
| P1 gate | GitGuardian still evaluates an older PR head that must be removed or explicitly resolved. | Current seed scanning passes locally and the curated artifacts contain no real PII or credentials. | Rewrite the old feature-branch head safely, push with lease, resolve the incident, and obtain a green GitGuardian check. |
| P1 gate | The development Vercel Preview has not been accepted against an isolated non-production Supabase branch. | The earlier July 22 production build baseline passed locally; production services and `main` remain untouched. | Provision the approved development branch, set Preview-only variables, deploy the merged development SHA, and verify the non-production invariant and browser suite. |
| P1 gate | The latest OAuth/profile-link/report hardening delta has not yet received a post-change production build and complete private-plugin unit rerun. | The 190-migration/1,279-assertion replay, 73/73 focused Bun tests, root typecheck, focused ESLint, plugin-isolation smoke, 14/14 role target, and 26-pass full CSF Playwright run are green. | Run the production build, complete private-plugin suite, and workflow/scale gates against the exact final source state. |
| P2 | Some screenshot footers show a transient `Checking` health badge. | Availability claims rely on the health endpoint/browser tests rather than screenshot timing. | Wait for the asynchronous status request before future screenshots or display a stable skeleton state. |
| P3 | The green production-mode run emits the Next.js diagnostic `Unexpected root span type AppRender.fetch`. | The run completes in 52.8 seconds with 26 passed, 3 intentional opt-in capture skips, 0 failed, and no application exception. | Monitor framework updates and investigate if it becomes an application error, request failure, or repeated production-observability issue. |
| P3 | A large gallery sweep can trigger a Next.js development memory restart. | Capture navigation retries once; the curated gallery, production build, and final sequential production browser run are complete. | Investigate only if the behavior reproduces outside the development runner. |
| P2 | Visual evidence cannot prove role denial or atomic writes. | Role and transaction assertions are kept in browser, server, and database test suites; the latest 1,279-assertion replay includes signed/manual profile-link stale retries and cohort/link-type enforcement, accepted-application locking, validated tenant foreign keys, immutable closure evidence, legacy-close revocation, nine evidence-write guards, and a real `dblink` two-session close-vs-insert race. Raw sensitive artifacts are excluded. | Require the final CI gates and attach their sanitized pass/fail summary. |
| P3 | Long application, import, semester, report, and staff pages require vertical scrolling. | Full-page screenshots preserve the complete surface and compact list rows avoid nested-card walls. | Continue keyboard, focus, and mobile usability checks on long records. |
| P3 | The gallery captures representative routes, not every modal or destructive confirmation. | The action matrix identifies which behaviors are visual versus separately tested. | Add only sanitized synthetic screenshots for any acceptance-critical missing modal after its workflow is stable. |
| P3 | Composite tenant foreign keys can make implicit PostgREST relationship embeds ambiguous. | The browser found the onboarding/cohort ambiguity; private-plugin commit `7f12388` uses explicit constraint embeds and adds a regression guard. The final production-mode run is green. | Keep explicit constraint embeds and the guard whenever additional foreign keys touch these relations. |
| P2 gate | The complete visible mutation lifecycle, keyboard/focus/screen-reader acceptance, and three native Google Slides decks are not complete. | Database, unit, role-boundary, production browser, and sanitized visual evidence are reported separately without upgrading those scopes. | Execute and document each remaining lifecycle and accessibility action, then create and visually verify the decks from final sanitized evidence. |
| P2 gate | The report archive service is locally tested, but a complete visible term-report download was not exercised in the end-to-end mutation lifecycle. | Reports are local permission-checked ZIP files with formula-safe CSV and a manifest; there is no Google write destination. | Execute a synthetic report download for each authorized/denied role and verify contents against aggregate queries. |
| P1 containment | Another local project is active on this machine. | CSF replay and cleanup require the Let’s Assist isolation marker, exact local ports/keys, and label-scoped resources. The final 190-migration replay did not access or reuse Vela. | Keep the isolation checks mandatory in local scripts and CI; stop immediately if project identity or ports do not match. |
| P1 external gate | Three final CSF Playwright scenarios remain intentionally skipped because they require live Google consent/configuration. | The non-Google suite passes 26/26, and no chooser, consent, Picker, Drive read/import, refresh, reconnect/revocation, or write was attempted. | Obtain action-time authorization, confirm the chapter Google identity, and rerun only the gated scenarios without capturing account or student data. |

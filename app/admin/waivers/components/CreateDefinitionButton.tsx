"use client";

import { Button } from "@/components/ui/button";

export function CreateDefinitionButton() {
  return (
    <Button type="button" variant="secondary" disabled title="Creation flow is not wired up yet">
      Create definition
    </Button>
  );
}
# Geist email font

`Geist-Variable.woff2` comes from the repository's pinned `geist` package. Activity emails use this public URL so the preview and email clients that support web fonts can use the same typeface as the app. Other clients use Arial.

The accompanying license is copied from that package. Keep both files together when updating the font.
